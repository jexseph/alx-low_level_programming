#include <stdio.h>
/**
 * main - entry point
 * single digits are to be printed
 * Return: 0 when sucessful
 */
int main(void)
{
int x = 48;
while (x < 58)
{
putchar (x);
x++;
}
putchar (10);
return (0);
}
